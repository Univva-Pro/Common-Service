# CommanFrontend

This project was generated with Angular CLI.

## Development server

Run `npm start` for a dev server. Navigate to `http://localhost:4203/`.

## Build

Run `npm run build` to build the project.
